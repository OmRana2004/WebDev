
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// we created a schema form here

model User {
  id         Int   @default(autoincrement())   @id
  username   String
  password   String  
}